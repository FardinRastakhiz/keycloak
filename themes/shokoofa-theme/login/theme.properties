parent=keycloak.v2
import=common/keycloak

# Re-declare parent's stylesheet (resolved via theme fallback) and layer our own on top
styles=css/styles.css css/shokoofa.css

# Favicon (type is inferred from the extension)
favicons.svg=img/icon.svg

scripts=js/theme-toggle.js

darkMode=true
